# 令和の虎｜年代MAP（学年版・一行表示＋年選択モーダル）

- 一覧は**年代ごとに一行**で横並び。ゼブラ行で視認性UP。
- 年の検索は「年を選ぶ」ボタン → モーダルでスクロール選択（59〜99, 00）。
- 名前検索、昇降順、配色切替対応。黄×赤の高コントラスト配色。

## 公開
1. このフォルダを新規リポジトリへ（例: `reiwa-no-tora-age-map`）
2. Settings → Pages → `Deploy from a branch` → `main / root`
