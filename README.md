# 令和の虎｜年代MAP（学年版・一行表示）

- 一年ごとに**一行だけ**の横並び。名前は横スクロールで表示（折返しなし）。
- GitHub Pages で即公開可。非公式。ロゴ未使用。

## 公開手順
1. このフォルダを新規リポジトリへ（例: `reiwa-no-tora-age-map`）
2. Settings → Pages → `Deploy from a branch` → `main / root`

## データ更新
- `data.js` を編集。キーは "59"〜"99" と "00"。
